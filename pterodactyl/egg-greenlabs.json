{
    "_comment": "GreenLabs - servidor de sinalizacao. https://github.com/gustavo-blacknaut/greenlabs-server",
    "meta": {
        "version": "PTDL_v2",
        "update_url": null
    },
    "exported_at": "2026-08-26T22:20:12.048Z",
    "name": "GreenLabs — Servidor de Sinalização",
    "author": "gustavo.c.raichardt@gmail.com",
    "description": "Servidor de sinalização do GreenLabs, para transmissão de tela e chamadas sem conta. Com o SFU ligado o vídeo passa por aqui e a travessia de roteador deixa de ser problema; desligado, o servidor só apresenta as pessoas umas às outras e gasta quase nada.",
    "features": null,
    "docker_images": {
        "Debian 12": "ghcr.io/parkervcp/yolks:debian"
    },
    "file_denylist": [],
    "startup": "chmod +x greenlabs-server 2>/dev/null; if [ -s greenlabs-server ] && bash -c \"./greenlabs-server --help\" >/dev/null 2>&1; then echo \"GreenLabs pronto, reaproveitando o que ja esta aqui\"; else A=amd64; case $(uname -m) in aarch64|arm64) A=arm64;; esac; R=https://github.com/gustavo-blacknaut/greenlabs-server/releases; N=greenlabs-server-linux-$A; V={{VERSAO}}; case \"$V\" in v*) U=$R/download/$V/$N;; *) U=$R/latest/download/$N;; esac; echo \"GreenLabs baixando $N...\"; rm -f greenlabs-server; B() { curl -fsSL -o greenlabs-server \"$1\" 2>/dev/null || wget -qO greenlabs-server \"$1\" 2>/dev/null; }; B \"$U\" || { echo \"  nao achei nessa versao; tentando a mais recente\"; B \"$R/latest/download/$N\"; } || { echo \"ERRO: nao consegui baixar; confira a internet do host\"; exit 1; }; chmod +x greenlabs-server; bash -c \"./greenlabs-server --help\" >/dev/null 2>&1 || { echo \"ERRO: o arquivo baixado nao executa nesta maquina\"; exit 1; }; echo \"GreenLabs instalado\"; fi; P={{PORTA}}; [ -z \"$P\" ] && P={{SERVER_PORT}}; F=; [ \"{{SFU}}\" = 1 ] && F=--sfu; exec ./greenlabs-server --port $P $F",
    "config": {
        "files": "{}",
        "startup": "{\"done\":\"Servidor de Sinalizacao GreenLabs rodando\"}",
        "logs": "{}",
        "stop": "^C"
    },
    "scripts": {
        "installation": {
            "script": "#!/bin/bash\n# Instalação do GreenLabs no Pterodactyl.\n#\n# Só adianta o download para o primeiro boot não esperar. Se falhar aqui, o\n# comando de inicialização baixa sozinho — por isso este script não aborta a\n# instalação por nada: um egg cuja instalação falha deixa o servidor inutilizado\n# no painel, e aqui não há nada que justifique isso.\n#\n# Não usa a API do GitHub nem jq de propósito. A API tem limite de 60 chamadas\n# por hora por IP quando não autenticada, e num host compartilhado esse limite\n# é dividido com estranhos. O endereço /releases/latest/download/NOME\n# redireciona sozinho para a release mais nova, sem chamada de API.\n\ncase \"$(uname -m)\" in\n  aarch64 | arm64) ARQUIVO=\"greenlabs-server-linux-arm64\" ;;\n  *) ARQUIVO=\"greenlabs-server-linux-amd64\" ;;\nesac\n\nVERSAO=\"${VERSAO:-latest}\"\nif [ \"${VERSAO}\" = \"latest\" ] || [ -z \"${VERSAO}\" ]; then\n  BASE=\"https://github.com/gustavo-blacknaut/greenlabs-server/releases/latest/download\"\nelse\n  BASE=\"https://github.com/gustavo-blacknaut/greenlabs-server/releases/download/${VERSAO}\"\nfi\n\necho \"GreenLabs — baixando ${ARQUIVO} (${VERSAO})\"\necho \"  ${BASE}/${ARQUIVO}\"\n\n# curl ou wget: qual existe varia com a imagem de instalação, e depender de um\n# só faz a instalação morrer numa linha que não explica nada.\nif command -v curl >/dev/null 2>&1; then\n  curl -fsSL --retry 3 --retry-delay 2 -o /mnt/server/greenlabs-server \"${BASE}/${ARQUIVO}\"\nelif command -v wget >/dev/null 2>&1; then\n  wget -q --tries=3 -O /mnt/server/greenlabs-server \"${BASE}/${ARQUIVO}\"\nelse\n  echo \"  nem curl nem wget nesta imagem\"\nfi\n\nif [ -s /mnt/server/greenlabs-server ]; then\n  chmod +x /mnt/server/greenlabs-server\n  echo \"  ok, $(stat -c%s /mnt/server/greenlabs-server) bytes\"\n  /mnt/server/greenlabs-server --help 2>/dev/null | head -1\nelse\n  # Arquivo vazio atrapalha mais que arquivo nenhum: o startup veria que existe\n  # e não baixaria de novo.\n  rm -f /mnt/server/greenlabs-server\n  echo \"  nao deu para baixar agora; o servidor baixa sozinho ao ligar\"\nfi\n\necho \"GreenLabs — instalacao concluida\"\nexit 0\n",
            "container": "ghcr.io/parkervcp/installers:debian",
            "entrypoint": "bash"
        }
    },
    "variables": [
        {
            "name": "Retransmitir o vídeo pelo servidor (SFU)",
            "description": "Ligado (1): cada pessoa mantém uma conexão só, com este servidor, que recebe o vídeo uma vez e reenvia para os outros. Quem transmite para de subir o vídeo várias vezes e ninguém precisa atravessar o roteador do outro. Custa banda e CPU daqui.\n\nDesligado (0): o servidor só apresenta as pessoas e sai da frente. O vídeo vai direto entre elas e este servidor gasta quase nada — mas se a conexão direta falhar, todo mundo aparece na lista e a tela fica preta, sem mensagem de erro.\n\nHospedando num painel, deixe ligado.",
            "env_variable": "SFU",
            "default_value": "1",
            "user_viewable": true,
            "user_editable": true,
            "rules": "required|string|in:0,1",
            "field_type": "text"
        },
        {
            "name": "Porta",
            "description": "Deixe em branco para usar a porta que o painel alocou — é o caso normal.\n\nPreencha só se quiser outra. Ela precisa estar liberada para este servidor, senão ele sobe e ninguém consegue chegar nele.",
            "env_variable": "PORTA",
            "default_value": "",
            "user_viewable": true,
            "user_editable": true,
            "rules": "nullable|integer|between:1,65535",
            "field_type": "text"
        },
        {
            "name": "Versão",
            "description": "Deixe em \"latest\" para a release mais recente. Para fixar uma versão, use a tag exata, com o \"v\" na frente: v0.2.0.\n\nQualquer outro valor cai na mais recente — nome de branch não vale aqui, porque o que se baixa é uma release, não o código.",
            "env_variable": "VERSAO",
            "default_value": "latest",
            "user_viewable": true,
            "user_editable": true,
            "rules": "required|string|max:40",
            "field_type": "text"
        }
    ]
}
