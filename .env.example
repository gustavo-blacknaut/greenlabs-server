# Porta em que o servidor de sinalização escuta.
# Use a porta que a sua hospedagem tem aberta.
PORT=3001

# O Pterodactyl entrega a porta alocada nesta variável. Se PORT não existir,
# o servidor usa SERVER_PORT sozinho — não precisa configurar nada lá.
# SERVER_PORT=25640
